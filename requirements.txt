# Cybersecurity Nexus - Python dependencies
# Pins are minimums; pip will pick the newest wheel that matches your Python.
# Recommended Python: 3.11 or 3.12 (best wheel coverage). 3.13/3.14 work too,
# just make sure pip is up to date so it can find the latest wheels.
# Upper bounds guard against breaking major-version releases.
neo4j>=5.20,<6
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
httpx>=0.27,<1
python-dotenv>=1.0,<2
python-multipart>=0.0.9,<1   # required by FastAPI for UploadFile / Form (SBOM, policy upload)
pydantic>=2.10,<3            # 2.10+ ships prebuilt wheels for Python 3.13/3.14
rich>=13.7,<15
python-docx>=1.1,<2          # for HIPAA SRA + Model Card docx output (optional fallback to .md)
pyyaml>=6.0,<7               # required by `python -m ingest.ai_threats --refresh` (live MITRE ATLAS)
